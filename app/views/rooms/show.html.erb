<div class="user_name">
 <% @entries.each do |e| %>
 <h2><%= e.user.name %>さんとのチャット</h2>
 <% end %>
</div>
<div class = "posts">
 <% form_with model: @message,method: :post do %>
 <%= f.text_field :body %>
 <%= f.hidden_field :room_id , value: @room.id %>
 <%= f.submit'送信' %>
</div>